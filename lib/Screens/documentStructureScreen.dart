import 'package:flutter/material.dart';

class Documentstructurescreen extends StatelessWidget {
  const Documentstructurescreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Document Structure'),
      ),
      body: Container(),
    );
  }
}
